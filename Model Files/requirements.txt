# WLASL-100 ASL Recognition - Python Dependencies
# Install with: pip install -r requirements.txt
#
# PyTorch: install separately from https://pytorch.org/
# (choose CUDA 12.x for RTX 3070)
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Core
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24
opencv-python>=4.8
tqdm>=4.65

# MediaPipe - IMPORTANT: use 0.9.x for the legacy solutions API (recommended)
# If 0.9.x is unavailable, use 0.10.x with the tasks API
# (requires downloading .task model files - see 1_extract_keypoints.py)
mediapipe==0.9.3

# Evaluation & visualisation
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3

# ONNX export / validation (optional but recommended)
onnx>=1.14
onnxruntime>=1.16
