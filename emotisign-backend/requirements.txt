# EmotiSign Backend — Python 3.9/3.10 compatible
# Install with: pip install -r requirements.txt

# Web framework
fastapi
uvicorn[standard]

# File uploads (multipart/form-data)
python-multipart

# JWT authentication
python-jose[cryptography]

# Password hashing
passlib[bcrypt]
bcrypt

# Database (async SQLite)
sqlalchemy
aiosqlite

# Pydantic v2 + settings
pydantic[email]
pydantic-settings

# Async file I/O
aiofiles

# ML Dependencies for WLASL-100 Model
# NumPy - MUST be installed first for compatibility
numpy

# PyTorch (CPU version - for GPU, see README)
torch
torchvision

# MediaPipe for keypoint extraction 
mediapipe

# OpenCV for video processing 
opencv-python


